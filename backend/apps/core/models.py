from django.db import models

# Core infrastructure models
# This file is initially empty as the core app is primarily for
# infrastructure-level migrations (like pgvector extension enablement).
# Feature-specific models should be defined in their respective apps.
